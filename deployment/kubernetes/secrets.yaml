apiVersion: v1
kind: Secret
metadata:
  name: morphml-secrets
  namespace: morphml
type: Opaque
stringData:
  # PostgreSQL credentials
  postgres-username: morphml
  postgres-password: changeme-in-production
  
  # Redis password
  redis-password: changeme-in-production
  
  # S3/MinIO credentials
  s3-access-key: minioadmin
  s3-secret-key: changeme-in-production
